import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuodominio.formulaguess',
  appName: 'FormulaGuess',
  webDir: 'out'
};

export default config;
